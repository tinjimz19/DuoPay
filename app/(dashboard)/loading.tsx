import { PageSkeleton } from "@/components/page-skeleton";

// Cubre todas las páginas del grupo: Inicio, Cobranza, Ventas, Clientes,
// Inventario, Pedidos, Reportes y Papelera.
export default function DashboardLoading() {
  return <PageSkeleton />;
}
